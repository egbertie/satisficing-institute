# duckdb-cli-ai-skills

DuckDB CLI integration for SQL analytics and data processing with AI assistance.

## Usage

Use DuckDB for fast analytical queries:

```bash
duckdb -c "SELECT * FROM 'file.csv' LIMIT 10"
```